// Global Execution Context
// Set breakpoint on line 7 in DevTools

var globalVar = "Hello Global";
let globalLet = 42;
const globalConst = { name: "Execution" };

debugger; // Set breakpoint here
console.log(globalVar, globalLet, globalConst);
